/**
 * Types for shared/creator-key.js. The implementation is plain ESM JavaScript so
 * that the bare-`node` pipeline entry point can import it; see the docblock there.
 */
export declare function deriveCreatorKey(videoUrl: string | null | undefined): string | null;
